#!/bin/zsh
# vim: ft=bash

set -e
set -o pipefail

title() {
    if command -v "boxify" &>/dev/null; then
        boxify title "$@"
    else
        echo "$@"
    fi
}

subtitle() {
    if command -v "boxify" &>/dev/null; then
        boxify subtitle "$@"
    else
        echo "$@"
    fi
}

show_help() {
    echo "Usage: $SCRIPT_NAME [OPTION]"
    echo "Run system updates for various package managers."
    echo
    echo "Options:"
    echo "  -h, --help, help    Show this help message and exit"
    echo "  -q, --quiet         Disable info and verbose output, show only errors and warnings"
    echo "  --only LIST         Run only stages in comma list (e.g. cargo,go,pip)"
    echo "  --skip LIST         Skip stages in comma list (e.g. neovim,zinit)"
    echo "  --json              Print machine-readable stage summary"
    echo "  pamac               Update using Manjaro 'pamac'"
    echo "  pacman-mirrors      Update Arch 'pacman-mirrors'"
    echo "  snap                Update using snap"
    echo "  flatpak             Update using flatpak"
    echo "  hardware            Update hardware firmware"
    echo "  clam                Update ClamAV database"
    echo "  clam-scan           Use preconfigured ClamAV scan settings"
    echo "  cargo               Update cargo packages"
    echo "  mise                Update dev run-time's with mise (rustup, nodenv, ...)"
    echo "  go                  Update go packages"
    echo "  gem                 Update ruby packages"
    echo "  pnpm                Update pnpm packages"
    echo "  bun                 Update bun runtime and global packages"
    echo "  pip                 Update pipx packages"
    echo "  zinit               Update zinit plugins"
    echo "  clean-dev           Clean (nvim, zinit, treesitter, mason) dev packages"
    echo "  neovim              Update Neovim plugins"
    echo "  bins                Update linked bins and sbins"
    echo "  completions         Generate Zsh completions and save to ~/.zfunc"
    echo "  and-shutdown        Run all updates and then shutdown the system"
}

QUIET=false
JSON_SUMMARY=false
LOCK_FD=9
LOCK_FILE="${XDG_RUNTIME_DIR:-/tmp}/updater.lock"
SCRIPT_NAME="$(basename "$0")"

typeset -a ONLY_STAGES=()
typeset -a SKIP_STAGES=()
typeset -a ALL_STAGES=(
    pamac
    pacman-mirrors
    snap
    flatpak
    hardware
    clam
    clam-scan
    mise
    cargo
    go
    gem
    pnpm
    bun
    pip
    zinit
    clean-dev
    neovim
    bins
)

typeset -a STAGES_SUCCESS=()
typeset -a STAGES_FAILED=()
typeset -a STAGES_SKIPPED=()
typeset -a NORMALIZED_STAGE_LIST=()

trim_whitespace() {
    local value="$1"
    value="${value#"${value%%[![:space:]]*}"}"
    value="${value%"${value##*[![:space:]]}"}"
    printf '%s' "$value"
}

normalize_stage_list() {
    local mode="$1"
    shift

    local stage trimmed
    NORMALIZED_STAGE_LIST=()

    for stage in "$@"; do
        trimmed="$(trim_whitespace "$stage")"
        [[ -z "$trimmed" ]] && continue

        if list_contains "$trimmed" "${ALL_STAGES[@]}"; then
            NORMALIZED_STAGE_LIST+=("$trimmed")
        else
            subtitle "Warning: unknown stage '$trimmed' in --$mode. Ignoring it." >&2
        fi
    done
}

list_contains() {
    local needle="$1"
    shift
    local item
    for item in "$@"; do
        [[ "$item" == "$needle" ]] && return 0
    done
    return 1
}

stage_selected() {
    local stage="$1"

    if (( ${#ONLY_STAGES[@]} > 0 )) && ! list_contains "$stage" "${ONLY_STAGES[@]}"; then
        return 1
    fi

    if list_contains "$stage" "${SKIP_STAGES[@]}"; then
        return 1
    fi

    return 0
}

record_stage_success() {
    STAGES_SUCCESS+=("$1")
}

record_stage_failure() {
    STAGES_FAILED+=("$1")
}

record_stage_skipped() {
    STAGES_SKIPPED+=("$1")
}

run_stage() {
    local stage="$1"
    local func="$2"

    if ! stage_selected "$stage"; then
        record_stage_skipped "$stage"
        return 0
    fi

    if "$func"; then
        record_stage_success "$stage"
    else
        record_stage_failure "$stage"
        subtitle "Warning: stage '$stage' failed. Continuing..." >&2
    fi
}

print_stage_summary() {
    if $JSON_SUMMARY; then
        local success_json="$(array_to_json "${STAGES_SUCCESS[@]}")"
        local failed_json="$(array_to_json "${STAGES_FAILED[@]}")"
        local skipped_json="$(array_to_json "${STAGES_SKIPPED[@]}")"
        printf '{"success":[%s],"failed":[%s],"skipped":[%s]}\n' \
            "$success_json" \
            "$failed_json" \
            "$skipped_json"
        return 0
    fi

    $QUIET || title "Update summary"
    echo "  success: ${#STAGES_SUCCESS[@]} (${STAGES_SUCCESS[*]:-none})"
    echo "  failed:  ${#STAGES_FAILED[@]} (${STAGES_FAILED[*]:-none})"
    echo "  skipped: ${#STAGES_SKIPPED[@]} (${STAGES_SKIPPED[*]:-none})"
}

json_escape() {
    local value="$1"
    value="${value//\\/\\\\}"
    value="${value//\"/\\\"}"
    value="${value//$'\n'/\\n}"
    value="${value//$'\r'/\\r}"
    value="${value//$'\t'/\\t}"
    printf '%s' "$value"
}

array_to_json() {
    local item escaped
    local output=""
    local separator=""

    for item in "$@"; do
        escaped="$(json_escape "$item")"
        output+="$separator\"$escaped\""
        separator=","
    done

    printf '%s' "$output"
}

acquire_lock() {
    if ! command -v flock &>/dev/null; then
        subtitle "Warning: flock not found. Running without updater lock." >&2
        return 0
    fi

    eval "exec ${LOCK_FD}>\"$LOCK_FILE\""
    if ! flock -n "$LOCK_FD"; then
        subtitle "Warning: another updater process is already running." >&2
        exit 1
    fi
}

check_command() {
    if ! command -v "$1" &>/dev/null; then
        subtitle "Warning: $1 not found. Skipping $2." >&2
        return 1
    fi
    return 0
}

check_last_clean() {
    local cache_file="${XDG_CACHE_HOME:-$HOME/.cache}/updater_last_clean"
    local current_time=$(date +%s)
    local week_seconds=$((7 * 24 * 60 * 60))
    local month_seconds=$((30 * 24 * 60 * 60))

    if [[ -f "$cache_file" ]]; then
        local last_clean=$(cat "$cache_file")
        local time_diff=$((current_time - last_clean))

        if [[ $time_diff -ge $month_seconds ]]; then
            echo "⚠️  WARNING: System cache not cleaned in $((time_diff / 86400)) days" >&2
            echo "   Run: sudo updater clean-dev" >&2
        elif [[ $time_diff -ge $week_seconds ]]; then
            echo "ℹ️  INFO: System cache last cleaned $((time_diff / 86400)) days ago" >&2
            echo "   Consider running: sudo updater clean-dev" >&2
        fi
    else
        echo "ℹ️  INFO: No clean history found. Consider running: sudo updater clean-dev" >&2
    fi
}

save_clean_timestamp() {
    local cache_file="${XDG_CACHE_HOME:-$HOME/.cache}/updater_last_clean"
    mkdir -p "$(dirname "$cache_file")"
    date +%s >"$cache_file"
    $QUIET || echo "✓ Clean timestamp saved"
}

update_flatpak() {
    if [[ $EUID -ne 0 ]]; then
        if check_command "flatpak" "flatpak update"; then
            $QUIET || title "Running: 'flatpak update -y'"
            flatpak update -y || true
        fi
    fi
}

update_pamac() {
    # Check if NOT running as root (sudo), and if so proceed
    if [[ $EUID -ne 0 ]]; then
        if check_command "pamac" "pamac update"; then
            $QUIET || title "Running 'pamac update --no-confirm --aur'"

            # Clear AUR build cache if update fails
            if ! pamac update --no-confirm --aur; then
                subtitle "⚠️  AUR update failed. Cleaning build cache and retrying..." >&2
                pamac clean --build-files --no-confirm
                pamac update --no-confirm --aur || true
            fi
        fi
    fi
}

update_mirrors() {
    if [[ $EUID -eq 0 ]]; then
        if check_command "pacman-mirrors" "mirror update"; then
            $QUIET || title "Refreshing pacman mirrors"
            pacman-mirrors --fasttrack 5 && pacman -Syy
        fi
    fi
}

update_snap() {
    if [[ $EUID -eq 0 ]]; then
        if check_command "snap" "snap update" && check_command "systemctl" "systemd operations" && check_command "pamac" "pamac snap update"; then
            $QUIET || title "Running 'pamac upgrade --enable-snap --no-confirm'"

            local update_status=0
            local snap_services_started=false
            if systemctl start snapd.apparmor.service snapd.service; then
                snap_services_started=true
            else
                subtitle "Warning: failed to start snap services. Skipping snap updates." >&2
                return 1
            fi

            if ! pamac update --enable-snap --no-confirm; then
                subtitle "Warning: snap update failed." >&2
                update_status=1
            fi

            if [[ "$snap_services_started" == true ]]; then
                systemctl stop snapd.apparmor.service snapd.service snapd.socket || true
            fi

            return $update_status
        else
            $QUIET || subtitle "No 'systemctl status snapd.apparmor.service snapd.service'"
            $QUIET || subtitle "pamac install snapd libpamac-snap-plugin"
        fi
    fi
}

update_hardware() {
    # Check if running as root (sudo), and if so proceed
    if [[ $EUID -eq 0 ]]; then
        if check_command "fwupdmgr" "firmware update"; then
            $QUIET || title "Running 'fwupdmgr refresh,get-updates,update'"
            fwupdmgr refresh || true
            fwupdmgr get-updates || true
            fwupdmgr update
        fi
    fi
}

update_clam() {
    # Check if running as root (sudo), and if so proceed
    if [[ $EUID -eq 0 ]]; then
        if check_command "freshclam" "ClamAV update"; then
            $QUIET || title "Running 'freshclam'"
            freshclam
        fi
    fi
}

clam_scan_and_quarantine() {
    # Quarantine all infected files found by ClamAV
    local quarantine_dir="/var/quarantine"
    if [[ $EUID -eq 0 ]]; then
        if check_command "clamscan" "ClamAV scan"; then
            $QUIET || title "Scanning entire system with ClamAV and quarantining infected files to $quarantine_dir"
            mkdir -p "$quarantine_dir"
            clamscan --recursive --infected --exclude-dir='^/sys|^/dev' --move="$quarantine_dir" /
        fi
    else
        subtitle "Warning: ClamAV quarantine scan requires root privileges." >&2
    fi
}

update_mise() {
    if [[ $EUID -ne 0 ]]; then
        if check_command "mise" "mise toolchain upgrade"; then
            $QUIET || title "Running: 'mise upgrade' (tool versions)"
            mise upgrade || true

            $QUIET || title "Running: 'mise plugins update'"
            mise plugins update || true
        fi
    fi
}

update_cargo() {
    # Check if running as user, and if so proceed
    # go install github.com/Gelio/go-global-update@latest
    if [[ $EUID -ne 0 ]]; then
        if check_command "cargo" "cargo packages update"; then
            $QUIET || title "Running: 'rustup update && cargo install-update -a'"
            local update_status=0
            rustup update || true
            if ! cargo install-update -a; then
                subtitle "Warning: cargo install-update failed. Installing cargo-update..." >&2
                cargo install cargo-update || true
                if ! cargo install-update -a; then
                    subtitle "Warning: cargo install-update failed again. Continuing with remaining updates." >&2
                    update_status=1
                fi
            fi

            return $update_status
        fi
    fi
}

update_go() {
    # Check if running as user, and if so proceed
    if [[ $EUID -ne 0 ]]; then
        if check_command "go" "go packages update"; then
            $QUIET || title "Running: 'go-global-update'"
            if ! go-global-update; then
                subtitle "Warning: go install-update failed." >&2
            fi
        fi
    fi
}

update_gem() {
    # Check if running as user, and if so proceed
    if [[ $EUID -ne 0 ]]; then
        if check_command "gem" "gem update"; then
            $QUIET || title "Running: 'gem update'"
            if ! gem update; then
                subtitle "Warning: gem update failed." >&2
            fi
        fi
    fi
}

update_pip() {
    # Check if running as user, and if so proceed
    if [[ $EUID -ne 0 ]]; then
        if check_command "uv" "uv tool updates"; then
            $QUIET || title "Running: 'uv tool upgrade --all'"
            uv self update || true
            uv tool upgrade --all || true
        fi
    fi
}

update_pnpm() {
    # Check if running as user, and if so proceed
    if [[ $EUID -ne 0 ]]; then
        if check_command "pnpm" "pnpm packages update"; then
            $QUIET || title "Running: 'pnpm update -g'"
            pnpm update -g || true
        fi
    fi
}

update_bun() {
    if [[ $EUID -ne 0 ]]; then
        if check_command "bun" "bun update"; then
            $QUIET || title "Running: 'bun upgrade && bun update (global)'"
            bun upgrade || true
            # Update global packages in their install dir
            (cd "$HOME/.bun/install/global" && bun update) || true
        fi
    fi
}

update_zinit() {
    # Check if running as user, and if so proceed
    if [[ $EUID -ne 0 ]]; then
        if check_command "zsh" "zinit update"; then
            $QUIET || title "Running: 'zsh -ic \"zinit self-update\",\"update -a\"'"
            if $QUIET; then
                zsh -f -ic "setopt no_monitor; source $HOME/.local/share/zinit/zinit.git/zinit.zsh && \
                zinit self-update --quiet  >/dev/null 2>&1 && \
                zinit update --all --parallel --quiet >/dev/null 2>&1 && \
                wait" >/dev/null 2>&1
            else
                zsh -f -ic "source $HOME/.local/share/zinit/zinit.git/zinit.zsh && \
                zinit self-update && \
                zinit update --all --parallel && \
                wait"
            fi
        fi
    fi
}

clean_dev() {
    if [[ $EUID -eq 0 ]]; then
        $QUIET || title "Cleaning system packages and logs"
        pamac clean --no-confirm
        pamac remove --orphans --no-confirm
        pamac clean --no-confirm --build-files
        journalctl --vacuum-time=2weeks
        find /var/log -type f -name '*.log' -delete
    else
        $QUIET || title "Removing cache and state data from development tools"
        local dirs_to_remove=(
            ~/.cache
            ~/.config/coc
            ~/.local/share/{coc,nvim,nvim-treesitter,zinit}
            ~/.vim/{bundle,plugged}
            ~/.zinit
        )
        rm -rf "${dirs_to_remove[@]}"

        local commands=(
            "pnpm:pnpm cache clean --force"
            "bun:bun pm cache rm"
            "go:GO111MODULE=off go clean -cache -modcache -i -r"
        )

        for cmd in "${commands[@]}"; do
            IFS=: read -r tool command <<<"$cmd"
            if check_command "$tool" "$command"; then
                $QUIET || echo "Cleaning $tool cache"
                eval "$command"
            fi
        done

        $QUIET || title "Running: 'mise prune'"
        mise prune || true
    fi
    save_clean_timestamp
}

link_bins() {
    $QUIET || title "Linking binaries from ~/usr/[bin|sbin] to [~/.local|/usr/local]/[bin|sbin]"

    local user_home target_bin target_sbin
    if [[ $EUID -eq 0 && -n "$SUDO_USER" ]]; then
        user_home="/home/$SUDO_USER"
        target_bin="/usr/local/bin"
        target_sbin="/usr/local/sbin"
    else
        user_home="$HOME"
        target_bin="$HOME/.local/bin"
        target_sbin="$HOME/.local/sbin"
    fi

    local dirs=("$target_bin" "$target_sbin")
    for dir in "${dirs[@]}"; do
        [[ -d "$dir" ]] || mkdir -p "$dir"
        find "$dir" -xtype l -delete
    done

    local src_dirs=("$user_home/usr/bin" "$user_home/usr/sbin")
    local dest_dirs=("$target_bin" "$target_sbin")
    for i in {0..1}; do
        if [[ -d "${src_dirs[$i]}" ]]; then
            find "${src_dirs[$i]}" -type f -executable -print0 |
                xargs -0 -I {} ln -sf {} "${dest_dirs[$i]}/"
        fi
    done
}

update_neovim() {
    # Check if running as user, and if so proceed
    if [[ $EUID -ne 0 ]]; then
        if check_command "nvim" "Neovim plugins update"; then
            $QUIET || title "Running: 'nvim Lazy update; TSUpdateSync' with 'timetout 20s'"
            if $QUIET; then
                nvim --headless \
                    -c 'lua require("lazy").update():wait()' \
                    -c 'Lazy load nvim-treesitter' \
                    -c 'TSUpdateSync' \
                    -c 'qa!' >/dev/null 2>&1
            else
                nvim --headless \
                    -c 'lua require("lazy").update():wait()' \
                    -c 'Lazy load nvim-treesitter' \
                    -c 'TSUpdateSync' \
                    -c 'qa!'
            fi
        fi
    fi
}

shutdown_system() {
    if check_command "shutdown" "system shutdown"; then
        $QUIET || title "All updates completed. Shutting down the system..."
        shutdown now
    fi
}

run_user_updates() {
    if $QUIET; then
        run_stage pamac update_pamac >/dev/null
        run_stage flatpak update_flatpak >/dev/null
        run_stage mise update_mise >/dev/null
        run_stage cargo update_cargo >/dev/null
        run_stage go update_go >/dev/null
        run_stage gem update_gem >/dev/null
        run_stage pnpm update_pnpm >/dev/null
        run_stage bun update_bun >/dev/null
        run_stage pip update_pip >/dev/null
        run_stage zinit update_zinit >/dev/null
        run_stage neovim update_neovim >/dev/null
        run_stage bins link_bins >/dev/null
    else
        $QUIET || title "Updating System (pamac, flatpak)"
        run_stage pamac update_pamac
        run_stage flatpak update_flatpak

        $QUIET || title "Updating Runtimes (mise)"
        run_stage mise update_mise

        $QUIET || title "Updating Libraries (cargo, go, gem, pnpm, bun, pip)"
        run_stage cargo update_cargo
        run_stage go update_go
        run_stage gem update_gem
        run_stage pnpm update_pnpm
        run_stage bun update_bun
        run_stage pip update_pip

        $QUIET || title "Updating terminal (zsh, nvim)"
        run_stage zinit update_zinit
        run_stage neovim update_neovim

        $QUIET || title "Re-linking local (s)binaries"
        run_stage bins link_bins
    fi
}

run_all_updates() {
    check_last_clean

    if [[ $EUID -eq 0 ]]; then
        # Root: hardware + clam + mirrors then regular updates
        if $QUIET; then
            run_stage pacman-mirrors update_mirrors >/dev/null
            run_stage hardware update_hardware >/dev/null
            run_stage clam update_clam >/dev/null
            run_stage snap update_snap >/dev/null
        else
            $QUIET || title "Updating System (mirrors, hardware, clam, snap)"
            run_stage pacman-mirrors update_mirrors
            run_stage hardware update_hardware
            run_stage clam update_clam
            run_stage snap update_snap
        fi

        # Now run user updates
        run_user_updates
    else
        # User mode: skip root stuff
        run_user_updates
    fi
}

time_function() {
    local start=$(date +%s)
    "$@"
    local end=$(date +%s)
    local duration=$((end - start))
    $QUIET || echo "⏱️  Completed in ${duration}s"
}

completions() {
    mkdir -p ~/.zfunc
    echo "#compdef updater" >~/.zfunc/_updater
    echo "" >>~/.zfunc/_updater
    echo "_updater() {" >>~/.zfunc/_updater
    echo "  local -a commands" >>~/.zfunc/_updater
    echo "  commands=(" >>~/.zfunc/_updater
    echo "    'pamac:Update using pamac'" >>~/.zfunc/_updater
    echo "    'pacman-mirrors:Update pacman-mirrors'" >>~/.zfunc/_updater
    echo "    'snap:Update using snap'" >>~/.zfunc/_updater
    echo "    'flatpak:Update using flatpak'" >>~/.zfunc/_updater
    echo "    'hardware:Update hardware firmware'" >>~/.zfunc/_updater
    echo "    'clam:Update ClamAV database'" >>~/.zfunc/_updater
    echo "    'clam-scan:Scan with pre-settings'" >>~/.zfunc/_updater
    echo "    'mise:Update all dev packages'" >>~/.zfunc/_updater
    echo "    'cargo:Update cargo packages'" >>~/.zfunc/_updater
    echo "    'go:Update go packages'" >>~/.zfunc/_updater
    echo "    'pnpm:Update pnpm packages'" >>~/.zfunc/_updater
    echo "    'bun:Update bun runtime and global packages'" >>~/.zfunc/_updater
    echo "    'pip:Update pipx packages'" >>~/.zfunc/_updater
    echo "    'zinit:Update zinit plugins'" >>~/.zfunc/_updater
    echo "    'clean-dev:Clean dev packages'" >>~/.zfunc/_updater
    echo "    'neovim:Update Neovim plugins'" >>~/.zfunc/_updater
    echo "    'bins:Update linked bins and sbins'" >>~/.zfunc/_updater
    echo "    'completions:Generate Zsh completions'" >>~/.zfunc/_updater
    echo "    'and-shutdown:Run all updates and then shutdown'" >>~/.zfunc/_updater
    echo "  )" >>~/.zfunc/_updater
    echo "  _arguments -s \\" >>~/.zfunc/_updater
    echo "    '(-h --help help)'{-h,--help}'[Show help message]' \\" >>~/.zfunc/_updater
    echo "    '(-q --quiet)'{-q,--quiet}'[Show only warnings and errors]' \\" >>~/.zfunc/_updater
    echo "    '--json[Print machine-readable stage summary]' \\" >>~/.zfunc/_updater
    echo "    '--only=[Run only listed stages (comma-separated)]' \\" >>~/.zfunc/_updater
    echo "    '--skip=[Skip listed stages (comma-separated)]' \\" >>~/.zfunc/_updater
    echo "    '*:command:_describe -t commands updater-commands commands'" >>~/.zfunc/_updater
    echo "}" >>~/.zfunc/_updater
    echo "" >>~/.zfunc/_updater
    echo "_updater \"\$@\"" >>~/.zfunc/_updater
    echo "Zsh completions have been saved to ~/.zfunc/_updater."
    echo "Please ensure ~/.zfunc is in your fpath and restart your shell or run 'autoload -U compinit && compinit' to enable them."
}

main() {
    local shutdown_after=false
    local only_flag_used=false
    while [[ $# -gt 0 ]]; do
        case $1 in
        -q | --quiet)
            QUIET=true
            shift
            ;;
        --json)
            JSON_SUMMARY=true
            shift
            ;;
        --only)
            shift
            if [[ -z "$1" ]]; then
                echo "Missing value for --only"
                exit 1
            fi
            only_flag_used=true
            local -a parsed_only
            parsed_only=(${(s:,:)"$1"})
            normalize_stage_list only "${parsed_only[@]}"
            ONLY_STAGES=("${NORMALIZED_STAGE_LIST[@]}")
            shift
            ;;
        --skip)
            shift
            if [[ -z "$1" ]]; then
                echo "Missing value for --skip"
                exit 1
            fi
            local -a parsed_skip
            parsed_skip=(${(s:,:)"$1"})
            normalize_stage_list skip "${parsed_skip[@]}"
            SKIP_STAGES=("${NORMALIZED_STAGE_LIST[@]}")
            shift
            ;;
        -h | --help | help)
            show_help
            exit 0
            ;;
        and-shutdown)
            shutdown_after=true
            shift
            ;;
        *)
            break
            ;;
        esac
    done

    if $only_flag_used && (( ${#ONLY_STAGES[@]} == 0 )); then
        subtitle "Warning: no valid stages found in --only. Nothing to run." >&2
        exit 1
    fi

    acquire_lock

    if [ $shutdown_after = true ]; then
        if $QUIET; then
            time_function run_all_updates >/dev/null
        else
            time_function run_all_updates
        fi
        shutdown_system
    elif [ $# -eq 0 ]; then
        if $QUIET; then
            time_function run_all_updates >/dev/null
        else
            time_function run_all_updates
        fi
    else
        for arg in "$@"; do
            case $arg in
            pamac)
                if $QUIET; then
                    run_stage pamac update_pamac >/dev/null
                else
                    run_stage pamac update_pamac
                fi
                ;;
            pacman-mirrors)
                if $QUIET; then
                    run_stage pacman-mirrors update_mirrors >/dev/null
                else
                    run_stage pacman-mirrors update_mirrors
                fi
                ;;
            snap)
                if $QUIET; then
                    run_stage snap update_snap >/dev/null
                else
                    run_stage snap update_snap
                fi
                ;;
            flatpak)
                if $QUIET; then
                    run_stage flatpak update_flatpak >/dev/null
                else
                    run_stage flatpak update_flatpak
                fi
                ;;
            hardware)
                if $QUIET; then
                    run_stage hardware update_hardware >/dev/null
                else
                    run_stage hardware update_hardware
                fi
                ;;
            clam)
                if $QUIET; then
                    run_stage clam update_clam >/dev/null
                else
                    run_stage clam update_clam
                fi
                ;;
            clam-scan)
                if $QUIET; then
                    run_stage clam-scan clam_scan_and_quarantine >/dev/null
                else
                    run_stage clam-scan clam_scan_and_quarantine
                fi
                ;;
            mise)
                if $QUIET; then
                    run_stage mise update_mise >/dev/null
                else
                    run_stage mise update_mise
                fi
                ;;
            cargo)
                if $QUIET; then
                    run_stage cargo update_cargo >/dev/null
                else
                    run_stage cargo update_cargo
                fi
                ;;
            go)
                if $QUIET; then
                    run_stage go update_go >/dev/null
                else
                    run_stage go update_go
                fi
                ;;
            gem)
                if $QUIET; then
                    run_stage gem update_gem >/dev/null
                else
                    run_stage gem update_gem
                fi
                ;;
            pip)
                if $QUIET; then
                    run_stage pip update_pip >/dev/null
                else
                    run_stage pip update_pip
                fi
                ;;
            pnpm)
                if $QUIET; then
                    run_stage pnpm update_pnpm >/dev/null
                else
                    run_stage pnpm update_pnpm
                fi
                ;;
            bun)
                if $QUIET; then
                    run_stage bun update_bun >/dev/null
                else
                    run_stage bun update_bun
                fi
                ;;
            zinit)
                if $QUIET; then
                    run_stage zinit update_zinit >/dev/null
                else
                    run_stage zinit update_zinit
                fi
                ;;
            clean-dev)
                if $QUIET; then
                    run_stage clean-dev clean_dev >/dev/null
                else
                    run_stage clean-dev clean_dev
                fi
                ;;
            bins)
                if $QUIET; then
                    run_stage bins link_bins >/dev/null
                else
                    run_stage bins link_bins
                fi
                ;;
            neovim)
                if $QUIET; then
                    run_stage neovim update_neovim >/dev/null
                else
                    run_stage neovim update_neovim
                fi
                ;;
            completions)
                completions
                exit 0
                ;;
            *)
                echo "Unknown update: $arg"
                show_help
                exit 1
                ;;
            esac
        done
    fi

    print_stage_summary
}

main "$@"
